{
  "name": "money-management-app",
  "version": "1.0.0",
  "description": "Multi-currency money management Android app",
  "main": "www/app.js",
  "scripts": {
    "dev": "npx http-server www -p 8080 -o -c-1",
    "sync": "npx cap sync",
    "build:android": "npx cap sync android && cd android && gradlew assembleDebug"
  },
  "keywords": [
    "money",
    "finance",
    "currency",
    "android"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "http-server": "^14.1.1"
  }
}